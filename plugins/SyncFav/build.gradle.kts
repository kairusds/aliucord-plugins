version = "1.0.1"
description = "Backports the sync feature for favorite emojis from the modern Discord clients."

aliucord {
    changelog.set(
        """
        # 1.0.1
        Use safer HTTP calls
        
        # 1.0.0
        * Initial release
        """.trimIndent()
    )
    deploy.set(true)
}