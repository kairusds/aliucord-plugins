version = "1.0.0"
description = "Backports the sync feature for favorite emojis from the modern Discord clients."

aliucord.changelog.set(
    """
    # 1.0.0
    * Initial release
    """.trimIndent()
)
